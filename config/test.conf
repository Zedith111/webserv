server {
	listen	127.0.0.1:8080 8081;
	server_name localhost;
	root /www;

	location / {
		autoindex off;
		root /test;
		index index.html;
		limit_except GET;
	}

	location /test {
		autoindex on;
		index index.html;
	}
}